#PBS -N node-uptime
#PBS -S /bin/bash
#PBS -V
#PBS -l nodes=3:ppn=4,walltime=00:03:00
tmp=""
while read ; do
  if [ "$tmp" != "$REPLY" ] ; then
    echo "$REPLY:"
    ssh -n -l nvetter $REPLY uptime | cut -f2,3 -d"," --complement
    tmp="$REPLY"
  fi
done < $PBS_NODEFILE
